# j2y
convert json to yaml 

# usage
```shell
Usage of ./j2y:
  -i string
    	input json file, default: stdin
  -o string
    	output yaml file, default: stdout
```  

```shell
cat a.json | j2y
```

